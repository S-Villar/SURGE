# M3DC1 Python Support Code

The `notebooks/m3dc1/m3dc1_python_code/` directory contains Python support code for M3DC1 workflows. It is **not** part of the SURGE repository and is listed in `.gitignore`.

## Status

- **Not included in SURGE** – the folder is excluded from version control.
- **Recipe TBD** – a recipe to pull this code from the actual M3DC1 repository will be documented in a future commit.

## How Was This Folder Obtained?

*To be documented.* The origin of `m3dc1_python_code` and how to obtain it from the external repository will be added here when we prepare the pull recipe.

## Usage

If you have the `m3dc1_python_code` directory in place (e.g. after cloning or symlinking from the external repo), the M3DC1 notebooks and scripts may import from it. See `notebooks/m3dc1/README.md` for usage.
