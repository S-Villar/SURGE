# Marker file so `python -m examples.quickstart` works from the repo root.
# The `examples/` tree is *not* installed as part of the `surge` wheel — it's
# a collection of runnable scripts kept alongside the source. See
# examples/quickstart.py for the main entrypoint.
